## What is a Module?

A **module** is a container for **multiple Terraform resources** that are used together.

Modules help you **organize, reuse, and manage infrastructure code** efficiently.

Every Terraform configuration has at least **one module called the Root Module**.

---

# Types of Terraform Modules

## 1. Root Module

The **main working directory** where you run Terraform commands.

Example structure:

```
project/  
 ├── main.tf  
 ├── variables.tf  
 ├── outputs.tf
```
This directory itself is the **root module**.

---

## 2. Child Modules

Modules **called inside another module**.

Example structure:

```
project/  
 ├── main.tf  
 ├── modules/  
 │   └── ec2/  
 │       ├── main.tf  
 │       ├── variables.tf  
 │       └── outputs.tf
```
The `ec2` folder becomes a **child module**.

---

# Module Block Syntax

Modules are called using a **module block**.

```
module "ec2_instance" {  
  source = "./modules/ec2"  
  
  instance_type = "t2.micro"  
  instance_name = "app-server"  
}
```

### Key Argument

|Argument|Description|
|---|---|
|`source`|Location of the module|

---

# Module Sources

Terraform modules can be loaded from **multiple locations**.

## 1. Local Path

source = "./modules/ec2"

Used for **modules inside the same project**.

---

## 2. Git Repository

source = "git::https://github.com/org/terraform-aws-ec2.git"

Used to **pull modules from Git repositories**.

---

## 3. Terraform Registry

source = "terraform-aws-modules/ec2-instance/aws"

Public modules from the **Terraform Registry**.

---

## 4. Specific Version (Registry)

```
module "ec2" {  
  source  = "terraform-aws-modules/ec2-instance/aws"  
  version = "5.0.0"  
}
```

Helps maintain **stable and predictable infrastructure**.

---

# Module Input Variables

Modules accept **input variables** defined in `variables.tf`.

Example:

### variables.tf (inside module)

```
variable "instance_type" {  
  type = string  
}
```

### Passing values
```
module "ec2_instance" {  
  source = "./modules/ec2"  
  
  instance_type = "t2.micro"  
}
```

---

# Module Outputs

Modules expose values using **outputs**.

### outputs.tf (inside module)

```
output "instance_id" {  
  value = aws_instance.app.id  
}
```

Accessing it in root module:

module.ec2_instance.instance_id

---

# Module Directory Structure (Best Practice)

```
modules/  
 └── ec2/  
     ├── main.tf  
     ├── variables.tf  
     ├── outputs.tf  
     ├── versions.tf  
     └── README.md
```
### File Purpose

|File|Purpose|
|---|---|
|main.tf|Main resources|
|variables.tf|Input variables|
|outputs.tf|Output values|
|versions.tf|Terraform & provider versions|
|README.md|Documentation|

---

# Module Benefits

## 1. Reusability

Write infrastructure once and reuse it across projects.

## 2. Organization

Break large infrastructure into **manageable components**.

## 3. Maintainability

Changes only need to be done **in one module**.

## 4. Standardization

Ensures consistent infrastructure deployment.

---

# Example Module

### Module (`modules/ec2/main.tf`)

```
resource "aws_instance" "app" {  
  ami           = "ami-123456"  
  instance_type = var.instance_type  
  
  tags = {  
    Name = var.instance_name  
  }  
}
```
### variables.tf
```
variable "instance_type" {  
  type = string  
}  
  
variable "instance_name" {  
  type = string  
}
```

---

### Root Module

```
module "web_server" {  
  source = "./modules/ec2"  
  
  instance_type = "t2.micro"  
  instance_name = "web-server"  
}
```

---

# Important Things to Know

### 1. Module Names Must Be Unique

Each module block must have a **unique name**.

---

### 2. Modules Support Meta-Arguments

Modules support:

- `depends_on`
    
- `for_each`
    
- `count`
    

Example:

```
module "servers" {  
  source = "./modules/ec2"  
  
  for_each = var.environments  
  
  instance_name = each.key  
}
```
---

### 3. Modules Do Not Share Variables Automatically

Variables must be **explicitly passed to modules**.

---

### 4. Modules Improve Large Infrastructure Management

Example real-world modules:

- VPC
    
- EC2
    
- Load Balancer
    
- RDS
    
- Security Groups
    

---

# Module Best Practices

|Practice|Why|
|---|---|
|Use separate modules for components|Improves structure|
|Always define variables and outputs|Improves usability|
|Use versioning|Prevents breaking changes|
|Keep modules small and focused|Better maintainability|

---

# Quick Summary

Terraform modules are **reusable infrastructure components** that allow you to organize, standardize, and scale Terraform configurations by grouping related resources together.